= form_for(@regist) do |f|
  - if @regist.errors.any?
    #error_explanation
      %h2
        = pluralize(@regist.errors.count, "error")
        prohibited this regist from being saved:
      %ul
        - @regist.errors.full_messages.each do |msg|
          %li= msg
  .actions
    = f.submit
