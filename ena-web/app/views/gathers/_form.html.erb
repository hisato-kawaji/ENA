<%= form_for(@gather) do |f| %>
  <% if @gather.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gather.errors.count, "error") %> prohibited this gather from being saved:</h2>

      <ul>
      <% @gather.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
